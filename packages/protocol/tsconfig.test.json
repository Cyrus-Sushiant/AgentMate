{
  // Type-checks the tests, which the build config leaves out so they never reach dist.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    "types": ["node"]
  },
  "include": ["src", "vitest.config.ts"],
  "exclude": []
}
